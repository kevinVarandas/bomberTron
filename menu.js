/**
 * Created by kevin on 22/02/2015.
 */

function showMenu(){
    ctx.save();
    ctx.drawImage(menuImage, 0, 0, w, h);
    ctx.restore();
}

function showPlayerSelection(){
    ctx.save();
    ctx.drawImage(menuImage, 0, 0, w, h);
    ctx.restore();
}